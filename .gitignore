# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# TypeScript incremental build cache
*.tsbuildinfo

# Secrets — never commit real env files
.env
.env.local
.env.*.local
api/.env
web/.env

# Cloudflare origin cert + private key (live only on the box, never in git)
deploy/cloudflare-origin.pem
deploy/cloudflare-origin.key
deploy/cloudflare-origin-pull-ca.pem

# Build artifacts
*.log
/api/bin/

# OS / editor
.DS_Store
*.swp
# Windows "mark of the web" alternate-data-stream files (created on WSL/Windows)
*:Zone.Identifier
.idea/
.vscode/
SECURITY-AUDIT.md
idea.md